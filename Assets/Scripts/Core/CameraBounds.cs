using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CameraBounds : MonoBehaviour
{
    public Vector2 bounds;

    GameManager gameManager;

    // void LateUpdate()
    // {
    //     transform.position = 
    //     // Bounds cBound = new Bounds(new Vector2(cameraBounds.x, 0), new Vector2(0, cameraBounds.y));
    //     // if (bounds.x - cBound.center.x < cBound.extents.x)
    //     // {
    //     //     // block camera movement
    //     // }
    // }
}